ANTHROPIC_API_KEY=         # Claude for extraction, reconciliation, recommendation agent, RAG alerts
DATABASE_URL=              # Neon PostgreSQL connection string (optional — runs without DB in demo mode)
